/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = {
    images: {
      unoptimized: true, // Disables sharp-based optimization
    },
  };

export default nextConfig;
